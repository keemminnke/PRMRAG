# Core dependencies
# CRITICAL: numpy must be <2.0 for vLLM + flash_attn compatibility!
torch>=2.0.0
transformers>=4.35.0
numpy<2.0,>=1.24.0  # Must be <2.0 for pyarrow/pandas compatibility with vLLM
pandas>=2.0.0,<2.4.0
pyarrow>=22.0.0,<23.0.0

# Configuration & validation
pydantic>=2.0.0
pyyaml>=6.0

# Utilities
tqdm>=4.66.0
jsonlines>=4.0.0
requests>=2.31.0

# LLM integration
openai>=1.3.0
anthropic>=0.8.0

# Training & Fine-tuning
datasets>=2.14.0
peft>=0.5.0
trl>=0.7.0

# Optional: Fast inference
# vLLM v0.13.0 tested and working with flash-attn 2.4.2
vllm>=0.13.0,<0.14.0
# guidance>=0.1.0

# Retrieval
rank-bm25>=0.2.2
wikipedia-api>=0.6.0
FlagEmbedding>=1.2.0

# Evaluation & metrics
scikit-learn>=1.3.0
scipy>=1.11.0

# Logging & monitoring
wandb>=0.15.0
tensorboard>=2.14.0

# Development
pytest>=7.4.0
black>=23.7.0
isort>=5.12.0
mypy>=1.5.0

# Optional performance optimizations
# flash-attn 2.4.2 tested and working with vLLM 0.13.0 + numpy<2.0
# WARNING: flash-attn installation requires CUDA and can take 10+ minutes to compile
flash-attn>=2.4.0,<2.5.0
ninja>=1.11.1.1
# sentence-transformers>=2.2.0  # Already included in FlagEmbedding
